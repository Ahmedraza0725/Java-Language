package calculatorwithexceptions;
/*import java.util.Scanner;
import java.util.Random;

public class RandomArrayIndex {
    public static void main(String[] args) {
        // Create an array with 100 random integers between 1 and 100
        int[] randomArray = new int[100];
        Random random = new Random();
        for (int i = 0; i < randomArray.length; i++) {
            randomArray[i] = random.nextInt(100) + 1; // Random numbers between 1 and 100
        }

        Scanner scanner = new Scanner(System.in);

        try {
            // Prompt the user for an index
            System.out.print("Enter an index (0 to 99): ");
            int index = scanner.nextInt();

            // Display the value at the specified index
            System.out.println("Value at index " + index + " is: " + randomArray[index]);

        } catch (ArrayIndexOutOfBoundsException e) {
            System.out.println("Error: Out of Bounds.");
        } catch (Exception e) {
            System.out.println("Invalid input. Please enter an integer.");
        } finally {
            scanner.close();
        }
    }
}
*/