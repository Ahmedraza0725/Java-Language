/*package lab03;
import java.util.ArrayList;
import java.util.Collections;

public class Cards {
    public static void main(String[] args) {
        // Step 1: Initialize the deck with numbers 0 to 51
        ArrayList<Integer> deck = new ArrayList<>();
        for (int i = 0; i < 52; i++) {
            deck.add(i);
        }

        // Step 2: Shuffle the deck
        Collections.shuffle(deck);

        // Step 3: Pick the first four cards after shuffling
        System.out.println("Four random cards picked from the deck:");
        for (int i = 0; i < 4; i++) {
            int card = deck.get(i);
            System.out.println("Card number: " + card);
        }
    }
}*/