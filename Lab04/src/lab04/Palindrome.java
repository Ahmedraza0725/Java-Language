/*package lab04;
import java.util.Scanner;

public class Palindrome{
    public static void main(String[] args) {
        // Scanner to get input from the user
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter a word in capital letters: ");
        
        // Read the input and make sure it's in uppercase
        String original = scanner.nextLine().toUpperCase();
        
        // Reverse the word
        String reversed = new StringBuilder(original).reverse().toString();
        
        // Check if the original word is the same as the reversed word
        if (original.equals(reversed)) {
            System.out.println("YES");
        } else {
            System.out.println("NO");
        }
        
        scanner.close();
    }
}*/
