/*package universitysystem;
public class ComputerComparison {
    public static void main(String[] args) {
        // Create objects of MyComputer and YourComputer
        MyComputer myComputer = new MyComputer();
        YourComputer yourComputer = new YourComputer();

        // Print processor details for MyComputer
        System.out.println("My Computer Processor Details:");
        myComputer.processor.printDetails();

        // Print processor details for YourComputer
        System.out.println("\nYour Computer Processor Details:");
        yourComputer.processor.printDetails();

        // Compare the processors
        System.out.println("\nDifferences Between Processors:");
        System.out.println("Type: " + myComputer.processor.type + " vs " + yourComputer.processor.type);
        System.out.println("Bandwidth: " + myComputer.processor.bandwidth + " vs " + yourComputer.processor.bandwidth);
        System.out.println("Speed: " + myComputer.processor.speed + " vs " + yourComputer.processor.speed);
        System.out.println("Processing: " + myComputer.processor.processing + " vs " + yourComputer.processor.processing);
    }
}*/
